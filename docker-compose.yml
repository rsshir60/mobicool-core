version: '3.8'

services:
  inference-api:
    build: .
    container_name: mobicool-inference-api
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=production
      - LOG_LEVEL=INFO
    volumes:
      - ./src/models/artifacts:/app/src/models/artifacts
    restart: unless-stopped
